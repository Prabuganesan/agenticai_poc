auto-install-peers = true
strict-peer-dependencies = false
prefer-workspace-packages = true
link-workspace-packages = deep
hoist = true
shamefully-hoist = true

# Allow installation to succeed even if optional dependencies fail to build
# Critical for cross-platform development (Linux prod, macOS/Windows dev)
optional-dependencies-fail-build = false
